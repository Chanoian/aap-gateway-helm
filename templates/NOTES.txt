{{ if .Release.IsInstall }}AAP Gateway installed.{{ else }}AAP Gateway upgraded.{{ end }}

  Release:    {{ .Release.Name }}
  Namespace:  {{ .Values.namespace }}
  Chart:      {{ .Chart.Name }}-{{ .Chart.Version }}
  Hostname:   {{ if .Values.hostname }}{{ .Values.hostname }}{{ else }}auto-generated by operator{{ end }}

Components:
  Controller:  {{ if ((.Values.controller).disabled) }}disabled{{ else }}enabled{{ end }}
  EDA:         {{ if ((.Values.eda).disabled) }}disabled{{ else }}enabled{{ end }}
  Hub:         {{ if ((.Values.hub).disabled) }}disabled{{ else }}enabled{{ end }}
  Redis mode:  {{ .Values.redis_mode | default "standalone" }}
  Database:    {{ if ((.Values.database).database_secret) }}external ({{ .Values.database.database_secret }}){{ else }}internal{{ end }}

Watch reconciliation:
  oc get aap {{ .Values.name | default "aap" }} -n {{ .Values.namespace }} -w

Check status:
  oc get aap {{ .Values.name | default "aap" }} -n {{ .Values.namespace }} -o jsonpath='{.status.conditions}'

Get the Gateway URL (once reconciled):
  oc get route -n {{ .Values.namespace }} -l app.kubernetes.io/managed-by=ansible-automation-platform-operator

Operator logs:
  oc logs -n {{ .Values.namespace }} -l control-plane=controller-manager --tail=50
{{- if eq ((.Values.secretProvider).type) "vso" }}

Secret provider: VSO (Vault Secrets Operator)
  Check VaultConnection / VaultAuth status:
    oc get vaultconnection,vaultauth -n {{ .Values.namespace }}
  Check VaultStaticSecret sync status (should show Valid=True once synced):
    oc get vaultstaticsecret -n {{ .Values.namespace }}
  Verify destination Secrets were created by VSO:
    oc get secret -n {{ .Values.namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
{{- end }}
{{- if .Values.ltm_route_enabled }}

LTM Route:
  oc get route {{ .Values.name | default "aap" }}-ltm -n {{ .Values.namespace }}
{{- end }}
