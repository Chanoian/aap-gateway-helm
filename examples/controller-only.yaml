# ── Controller only — minimal deployment ──────────────────────────────────────
# Gateway + AutomationController only. Hub and EDA explicitly disabled.
# Good for: getting started, CI environments, small teams.

name: aap
namespace: aap

no_log: false

controller:
  disabled: false

eda:
  disabled: true

hub:
  disabled: true
